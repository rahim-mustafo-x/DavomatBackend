# Database Configuration
DB_NAME=davomat_db
DB_USERNAME=davomat_user
DB_PASSWORD=changeme_secure_password
DB_URL=jdbc:postgresql://your_host_ip:5432/davomat_db
HOST_IP=your_host_ip              # Your server IP address (e.g., 192.168.1.150)
APP_PORT=8080                     # Application port

# Email Configuration (Gmail)
# Get app password from: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password

# JWT Secret (generate with: openssl rand -base64 64)
JWT_SECRET=change-this-to-a-secure-random-string-minimum-512-bits
